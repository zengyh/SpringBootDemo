
spring:
  datasource:
    url: jdbc:oracle:thin:@172.40.1.20:1521:matedb
    driverClassName: oracle.jdbc.driver.OracleDriver
    username: root
  jpa:
    database: ORACLE
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy

server:
  port: 80

